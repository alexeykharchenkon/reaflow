export interface NodeParams {
    inputsCount: string;
    outputsCount: string;
    maxInputsCount: string;
    maxOutputsCount: string;
    type: string;
    className: string;
    checked: boolean;
    text: string;
}